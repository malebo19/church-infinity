import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.churchinfinity.app',
  appName: 'church-infinity',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
